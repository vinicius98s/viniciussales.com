import styled from "styled-components";

const StyledHeader = styled.div`
    padding: 50px 0;
    position: relative;
`;

const Title = styled.h1`
    text-decoration: none;
    font-size: 80px;
    font-weight: 600;
`;

const Description = styled.h3`
    font-size: 20px;
`;

export { StyledHeader, Title, Description };
